{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${REFLEX_CLI_ROOT}/hooks/command-track.sh"
          }
        ]
      }
    ]
  }
}